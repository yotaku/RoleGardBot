{
  "name": "role-guard-bot",
  "version": "1.0.0",
  "description": "Discord bot that restricts messages in a channel unless the sender has a specific role.",
  "main": "bot.js",          // ← ここはエントリーファイル名
  "type": "module",          // ES Modules (`import ...`) を使うので必須
  "scripts": {
    "start": "node bot.js",  // Render の Start Command もこれ
    "dev": "nodemon bot.js"  // ローカル開発用（変更を自動再起動）
  },
  "engines": {
    "node": ">=18"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^14.14.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
